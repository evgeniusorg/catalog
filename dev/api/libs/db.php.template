<?php
  $mysqli = new mysqli("host", "login", "password", "base");
  if ($mysqli->set_charset("utf8")) {
        
  }
  if ($mysqli->connect_error) {
    error(400, "Connect Error", "Connect Error (" . $mysqli->connect_errno . ") " . $mysqli->connect_error);
  }
?>